#!/usr/bin/awk -F, -f

# generated by: awk-csvalid https://github.com/tesera/awk-csvalid

# awk-csvalid csv toolset generator: https://github.com/tesera/awk-csvalid
# usage:
#    validate:     awk -f action=validate validator.awk > violations.txt
#    create table: awk -v action=table -f validator.awk | psql afgo_dev
#    sanitize csv: awk -v action=sanitize -f validator.awk > sanitized.csv
#    insert sql:   awk -v action=insert -f validator.awk | psql afgo_dev

# awk is a simple unix text file parser: http://www.gnu.org/software/gawk/manual/gawk.html
# awk primer:
#    NR = number/index current record
#    RS = record seperator new line i.e. \n
#    FS = field seperator i.e. ,
#    /pattern/ { expression } = if pattern is truthy run expression

BEGIN {
    FS=","; OFS=","; err_count=0; cats["na"]=0;
    if(!action) action = "validate"
}

# builtin helper functions
function eql(x,y) {v=1; for (i in x) v=(v&&x[i]==y[i]); return v;}
function are_headers_valid(valid_headers) { v=0; split($0, h, ","); split(valid_headers, vh, "|"); return eql(h, vh); }
function is_unique(i, val) { if (vals[i,val]) { return 0; } else { vals[i,val] = 1; return 1; } }
function is_numeric(x){ return(x==x+0) }
function print_cats(categories) { for (category in categories) { if (categories[category]) print "      " category ": " categories[category]; } }
function log_err(cat) { cats[cat]++; err_count++; }


#get rid of the evil windows cr
{ sub("\r$", "") }


# make header index/map
NR > 1 {
    company_plot_number=$2
    measurement_number=$3
    regeneration_plot_name=$4
    species=$5
    regeneration_count=$6
    regeneration_comment=$7
    company=$1
}

# awk rules based on user csv ruleset
NR == 1 { headers="company|company_plot_number|measurement_number|regeneration_plot_name|species|regeneration_count|regeneration_comment"; if (!are_headers_valid(headers)) print "invalid headers in " FILENAME }
action == "validate" && NR > 1 && company == "" { log_err("warning"); print "Field company in " FILENAME " line " NR " is required" RS $0 RS; } 
action == "validate" && NR > 1 && company != "" && company !~ /^(AINS|GOA|APLY|ALPC|ANC|BLUE|CFPL|CFS|DAIS|FOFP|BUCH|MDFP|MWWC|SLPC|SPRA|SUND|SFPI|HLFP|TOLK|TOSL|UOA|VAND|WFML|WYGP|WYPM|UNKN|HPFP)$/ { log_err("warning"); print "company in " FILENAME " line " NR " should match the following pattern /^(AINS|GOA|APLY|ALPC|ANC|BLUE|CFPL|CFS|DAIS|FOFP|BUCH|MDFP|MWWC|SLPC|SPRA|SUND|SFPI|HLFP|TOLK|TOSL|UOA|VAND|WFML|WYGP|WYPM|UNKN|HPFP)$/ and was " company " " RS $0 RS; } 
action == "validate" && NR > 1 && company_plot_number == "" { log_err("warning"); print "Field company_plot_number in " FILENAME " line " NR " is required" RS $0 RS; } 
action == "validate" && NR > 1 && company_plot_number != "" && length(company_plot_number) > 15 { log_err("warning"); print "company_plot_number length in " FILENAME " line " NR " should be less than 15 and was " length(company_plot_number) " " RS $0 RS; } 
action == "validate" && NR > 1 && measurement_number && !is_numeric(measurement_number) { log_err("warning"); print "Field measurement_number in " FILENAME " line " NR " should be a numeric but was " measurement_number " " RS $0 RS; } 
action == "validate" && NR > 1 && measurement_number == "" { log_err("warning"); print "Field measurement_number in " FILENAME " line " NR " is required" RS $0 RS; } 
action == "validate" && NR > 1 && measurement_number != "" && length(measurement_number) > 2 { log_err("warning"); print "measurement_number length in " FILENAME " line " NR " should be less than 2 and was " length(measurement_number) " " RS $0 RS; } 
action == "validate" && NR > 1 && regeneration_plot_name == "" { log_err("warning"); print "Field regeneration_plot_name in " FILENAME " line " NR " is required" RS $0 RS; } 
action == "validate" && NR > 1 && regeneration_plot_name != "" && regeneration_plot_name !~ /^(R1|R2|R3|R4|R5|R6|R7|R8|R9|R10)$/ { log_err("warning"); print "regeneration_plot_name in " FILENAME " line " NR " should match the following pattern /^(R1|R2|R3|R4|R5|R6|R7|R8|R9|R10)$/ and was " regeneration_plot_name " " RS $0 RS; } 
action == "validate" && NR > 1 && species == "" { log_err("warning"); print "Field species in " FILENAME " line " NR " is required" RS $0 RS; } 
action == "validate" && NR > 1 && species != "" && species !~ /^(A|Aw|Pb|Bw|Ax|Dd|Dc|P|Pl|Pw|Pa|Pf|Pj|Px|Sw|Se|Sb|Sx|Fb|Fa|Fd|Lt|Lw|La|Ls|Du|Ms|No)$/ { log_err("warning"); print "species in " FILENAME " line " NR " should match the following pattern /^(A|Aw|Pb|Bw|Ax|Dd|Dc|P|Pl|Pw|Pa|Pf|Pj|Px|Sw|Se|Sb|Sx|Fb|Fa|Fd|Lt|Lw|La|Ls|Du|Ms|No)$/ and was " species " " RS $0 RS; } 
action == "validate" && NR > 1 && regeneration_count && !is_numeric(regeneration_count) { log_err("warning"); print "Field regeneration_count in " FILENAME " line " NR " should be a numeric but was " regeneration_count " " RS $0 RS; } 
action == "validate" && NR > 1 && regeneration_count != "" && regeneration_count < (species=="No"?0:1) { log_err("warning"); print "regeneration_count in " FILENAME " line " NR " should be greater than (species=="No"?0:1) and was " regeneration_count " " RS $0 RS; } 
action == "validate" && NR > 1 && regeneration_count != "" && regeneration_count > 9999 { log_err("warning"); print "regeneration_count in " FILENAME " line " NR " should be less than 9999 and was " regeneration_count " " RS $0 RS; } 

# sanitize rules
action ~ /^(sanitize|insert)$/ && NR > 1 {
    if (species == "") $5 = "NA"
    if (regeneration_count == "") $6 = "-9999"
    if (regeneration_plot_name == "") $4 = "NA"
    if (measurement_number == "") $3 = "NA"
    if (company_plot_number == "") $2 = "NA"
    if (regeneration_comment == "") $7 = "NA"
    if (company == "") $1 = "NA"
}

# action handlers
action == "insert" && NR == 1 {
    print "SET client_encoding = 'UTF8';"
    gsub("Range", "rangeno");
    print "COPY  (" $0 ") FROM stdin;"
}
action == "insert" && NR > 1 {
    gsub(",", "	");
    print;
}
action == "table" && NR == 1 {
     print "CREATE TABLE IF NOT EXISTS  (company_plot_number text,measurement_number text,regeneration_plot_name text,species text,regeneration_count numeric,regeneration_comment text,company text);"
}
action == "sanitize" { print }

# la fin
END {
    if (action == "insert") print "\\."
    if (action == "validate" && options["summary"] == "true") { print RS "violation summary: " RS "   counts:   " RS "      total: " err_count; print_cats(cats); }
}
